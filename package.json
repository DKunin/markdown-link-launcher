{
  "$schema": "https://raycast.com/schemas/extension.json",
  "name": "markdown-link-launcher",
  "title": "Markdown Link Launcher",
  "description": "Read markdown files and open links from them",
  "icon": "extension-icon.png",
  "author": "dkuninn",
  "categories": [
    "Productivity",
    "Documentation"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "open-markdown-links",
      "title": "Приборы",
      "description": "Read a markdown file and open links from it",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "markdownFilePath",
      "title": "Markdown File Path",
      "description": "Full path to your markdown file or directory",
      "type": "textfield",
      "required": true,
      "placeholder": "~/Documents/notes.md or ~/Documents/notes"
    },
    {
      "name": "searchRecursive",
      "title": "Search Recursively",
      "description": "Search through subdirectories when a folder path is provided",
      "type": "checkbox",
      "required": false,
      "label": "Enable recursive search",
      "default": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "eslint": "^9.13.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.4"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "eslint \"src/**/*.{ts,tsx}\" --fix",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "publish": "ray publish",
    "test": "vitest run"
  }
}
